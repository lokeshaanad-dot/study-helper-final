name: study-helper
version: "1.0"

tasks:
  - id: task_easy
    grader:
      type: llm
      prompt_template: "Score the output between 0.1 and 0.9"

  - id: task_medium
    grader:
      type: llm
      prompt_template: "Score the output between 0.1 and 0.9"

  - id: task_hard
    grader:
      type: llm
      prompt_template: "Score the output between 0.1 and 0.9"
