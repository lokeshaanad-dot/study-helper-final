[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "study-helper"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["fastapi", "uvicorn", "openenv-core>=0.2.0"]
